'use strict';
/* TODO */ function total(price) {
  const tax = 0.1;
  // TODO
  ('use strict');
  return price + price * tax;
}
document.getElementById('output').textContent =
  'コーヒーメーカーの値段は' + /* TODO */ total(8000) + '円（税込）です。';
